// Exericios Arduino
// Renato Alves Spada - 24783565

for ( int i = 0; i < tamanhoVetor ; i ++) {
 if ( sensorReadings [ i ] == 150) {
 break ;
  }
}

/* O código busca o valor 150 dentro do vetor sensorReadings.
   Assim que encontrar esse valor, ele para de procurar e sai do laço */
